[package]
name = "bevy_audio_controller_derive"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", default-features = false, features = [
  "full",
  "visit",
] }
quote = "1.0"
